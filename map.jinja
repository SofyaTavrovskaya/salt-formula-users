{% set users = salt['grains.filter_by']({
  'CentOS': {
    'root_group': 'root',
    'bash_package': 'bash',
    'sudo_package': 'sudo',
},
'Ubuntu': {
    'root_group': 'root',
    'bash_package': 'bash',
    'sudo_package': 'sudo',
},
}, merge=salt['pillar.get']('salt-formula-users:lookup')) %}
